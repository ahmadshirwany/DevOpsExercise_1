version: '3.9'
services:
  service1:
    build: ./service1
    depends_on:
      - storage
    ports:
      - "8001:8001"
  service2:
    build: ./service2
    depends_on:
      - storage
    
  storage:
    build: ./storage
    volumes:
      - data:/data

volumes:
  vstorage: {}
  data: {}